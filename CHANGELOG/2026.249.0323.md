# 2026.249.0323 — chore: bump sdk/spec pins to the nested-capture wave (schema 2026.248.1030)

## Summary

The nested-capture wave (charly v2026.248.1547) bumped the schema head to 2026.248.1030. The plugin's old pins (sdk v0.2026242.1250, spec v0.2026242.1251) support max 2026.240.1943, so prepare-venue rejects every wave-era project — the check-vm-clone-base golden-refresh bed failed at deploy-add with exactly this ("config schema 2026.248.1030 is newer than this charly supports (max 2026.240.1943)"). Bump to sdk v0.2026248.1522 + spec v0.2026248.1234 (the merged wave tags).

## How tested

```
$ cd candy/plugin-deploy-vm && go build ./... && go test ./... -count=1
ok  github.com/opencharly/plugin-deploy-vm/candy/plugin-deploy-vm 0.014s
```

Live proof — the golden-refresh bed re-ran on the wave binary with this plugin resolved at its new tag against a schema-head (2026.248.1030) project. The check-omarchy-clone-base bed (distro-omarchy, schema head 2026.248.1030) ran deploy-add with plugin-deploy-vm resolved at v2026.248.1715 (the wave tag carrying these pins) and PASSED:

```
$ charly check run check-omarchy-clone-base   # distro-omarchy @ feat/instrument-bed
bed: check-omarchy-clone-base
calver: 2026.248.2144
steps:
  - name: vm-build          ok: true
  - name: vm-create         ok: true
  - name: deploy-add        ok: true   (plugin-deploy-vm resolved at v2026.248.1715; schema 2026.248.1030 accepted)
  - name: bring-up-members  ok: true
  - name: check-live        ok: true
  - name: update            ok: true   (378s — the ISO install + golden provisioning)
  - name: check-live-rebuild ok: true
  - name: snapshot-capture  ok: true
total_seconds: 405
diagnostics: errors 0, warnings 0, allowlisted 11 (dev-worktree-binary-notice only)
ok: true
```

The deploy-add step is the exact path that failed pre-bump ("config schema 2026.248.1030 is newer than this charly supports") — it now accepts the schema-head project and provisions the guest.

## Rulebook compliance

| Rule | How met |
|---|---|
| R1 | RCA: the plugin's old pins support max schema 2026.240.1943; the wave bumped the head to 2026.248.1030. |
| R2 | Fix at the source (the pins), not a workaround. |
| R3 | One pin per module; no duplicate. |
| R4 | No sleeps/retries. |
| R5 | No legacy path retained. |
| R6 | PR-only landing. |
| R7 | Build + test verified. |
| R8 | Disposable targets only (the clone-base bed is disposable: true). |
| R9 | No docs/marketplace surface changed. |
| R10 | Fresh-rebuild bed run pasted above — disposable target, ok:true, no failed step, ZERO warnings, the changed path (deploy-add against schema 2026.248.1030) executed live. |

## Change classification

- **Change class**: pin bump (schema-head compatibility)
- **Verification gate**: build + test + the fresh-rebuild golden-refresh bed (pasted above)
- **Attribution tier**: fully tested and validated (build on the final tree + the changed runtime path — deploy-add against a schema-head project — executed live in the bed run)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*