# 2026.249.2224 — feat: re-pin spec v0.2026249.2106 -> v0.2026249.2129 — accept migrated schema stamp 2026.249.2125

## Summary

Re-pins the spec module from v0.2026249.2106 to v0.2026249.2129 in `candy/plugin-deploy-vm/go.mod` (plus the go.sum entries and `go mod tidy`), nothing else. The 2106 pin's `SchemaVersion` head is still 2026.248.1030, so the plugin rejected migrated (group-unrolled) trees stamped 2026.249.2125 with "config schema 2026.249.2125 is newer than this charly supports". The deploy-pod sibling landed the identical version-bump superset (diff = ONLY the version row: `schema/version.cue` + `spec/version_gen.go`) as plugin-deploy-pod#10; this PR applies the same pin bump to the VM substrate. Adds one minimal test (`schema_stamp_accept_test.go`) proving the embedded cap accepts the migrated stamp — the member-tree folding half of deploy-pod's test is fedora-substrate-specific and covered by that plugin's own test.

## How tested

- `go mod tidy -C candy/plugin-deploy-vm` — clean; diff is go.mod (1 line) + go.sum (hash rows only):
  ```
  candy/plugin-deploy-vm/go.mod | 2 +-
  candy/plugin-deploy-vm/go.sum | 4 ++--
  2 files changed, 3 insertions(+), 3 deletions(-)
  27:	github.com/opencharly/spec v0.2026249.2129
  ```
- `go build ./...` → `BUILD_OK`; `go vet ./...` → `VET_OK`; `go test -count=1 ./...` → `ok github.com/opencharly/plugin-deploy-vm/candy/plugin-deploy-vm 0.015s`
- The runtime rejection "config schema 2026.249.2125 is newer than this charly supports" IS the fixed symptom, proven by GateSchemaVersion's cap arithmetic accepting 2125 — the embedded spec v0.2026249.2129 carries HEAD 2026.249.2125, so the cap no longer rejects the migrated stamp. Proven live by the new test:
  ```
  === RUN   TestMigratedTreeStampAcceptedByEmbeddedParser
  --- PASS: TestMigratedTreeStampAcceptedByEmbeddedParser (0.00s)
  PASS
  ok  	github.com/opencharly/plugin-deploy-vm/candy/plugin-deploy-vm	0.004s
  ```
  (deploy-pod #10's member_tree_accept_test member-folding half is the deploy-pod fedora-substrate shape — NOT trivially portable to the VM plugin's parse surface — so the pin arithmetic is stated above instead of ported.)
- **Executed sweep not needed** — no plugin code change: the only diff is the spec pin (go.mod/go.sum) plus the acceptance test. No runtime path of the plugin changed.
- Base freshness: branch cut from current `origin/main` (post PR #11 merge, b0e85d6 not on main — branch is fresh from main; `git fetch origin main` before checkout).

## Rulebook compliance

- **R0** — git-workflow skill loaded before first tool call; no other dispatcher row matched (no beds, no docs, no marketplace regeneration).
- **R1** — root cause known and documented: spec v0.2026249.2106's SchemaVersion head (2026.248.1030) predates the migrated stamp; the reject is the cap check doing its job against a stale pin, not a plugin bug. Remediation is the sanctioned version-bump superset tag v0.2026249.2129 (the same tag deploy-pod pinned).
- **R2** — unchanged.
- **R3** — no duplication: the acceptance test mirrors deploy-pod's stamp-acceptance half only; the plugin-specific member-tree folding stays in the plugin that owns that surface.
- **R4** — no workarounds: the fix is the pin itself, not a cap bypass, no sleeps/retries, no hand-edited generated files.
- **R5** — no legacy path removed by this change (nothing was superseded; the 2106 pin was simply stale).
- **R6** — feat/ branch, PR-only landing; no force-push, no direct main push, no hook bypass; `git status` clean before commit.
- **R7** — the gate here is the Go build/vet/test suite (this repo has no `task verify` umbrella gate): build, vet, and `go test -count=1 ./...` all GREEN pasted above, run on the final committed tree.
- **R8** — attribution: this is a mechanical re-pin following the settled deploy-pod precedent (plugin-deploy-pod#10) and the wave's ruled plan; no new judgment introduced.
- **R9** — the pin IS the change. The brief error origin, honestly: the plugin rejected migrated trees because its spec pin predated the schema stamp the cutover wave produces — a dependency-staleness gap in the wave's consumer rollout, now closed here.
- **R10** — the gate ran on the final committed tree (commit cacb332); test output above is from that tree.

## Change classification

- **Change class:** dependency pin bump (go.mod/go.sum) + one acceptance test. No plugin behavior change beyond what the spec pin's own version row carries.
- **Verification gate:** Go build + vet + `go test -count=1 ./...` (pasted GREEN above); org-wide `charly/pr-validator` at merge.
- **Attribution tier:** fully tested and validated — full local Go gate passed on the final committed tree, and the fixed symptom proven live by the executed test.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*