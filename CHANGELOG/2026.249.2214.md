# 2026.249.2214 — feat: re-pin spec 2106 + sdk 2114 — member-tree cutover for the vm substrate

## Summary

Cutover C re-pin wave for the vm deploy substrate: re-pin spec v0.2026248.1234 → **v0.2026249.2106** (group: kind REMOVED from #ResourceKind, Deploy.IsGroup() deleted, the ONE ordered member list `Deploy.Member` with the position vocabulary replaces the dual `Children`/`Members` maps) and sdk v0.2026249.1109 → **v0.2026249.2114** (member-tree parse/fold). The plugin binaries previously rejected a migrated tree ("config schema 2026.249.2125 is newer") — the embedded spec parser now understands the member-tree node shape charly's CC-1 migration stamps.

Semantic migration (the only consumer of the removed dual-map surface was `vmPostApply`'s nested-pod walk):

- `node.Children` map (nested-in-venue children) → `node.InSubstrateMembers()` over the ordered member list, classified by the `in-substrate` POSITION — never a tree branch.
- The inline child guards moved into the extracted `inGuestPodMembers` helper (R3: one selection implementation, directly unit-testable): image-less members and android/kubernetes/vm members are not in-guest pods; deploy-level members stay host-side on the shared network.
- `sortedChildKeys` (map-key sort over the removed map) DELETED outright with the `sort` import (R5 — the ordered list carries authored order).
- The removed-shape node decodes to a member-less tree and is a noop — hard cutover, no dual-map resurrection.

## How tested

```
$ gofmt -l . && go build ./... && go vet ./... && go test -count=1 ./...
ok  	github.com/opencharly/plugin-deploy-vm/candy/plugin-deploy-vm	0.013s
?  	github.com/opencharly/plugin-deploy-vm/candy/plugin-deploy-vm/cmd/serve	[no test files]
```

Executed removed-identifier sweep on the COMMITTED tree (B14d):

```
$ git grep -nE 'IsGroup|SortedMemberKeys|SortedNestedKeys|ValidateDeploymentChildren|HasChildren|\.Children|\.Members|sortedChildKeys' -- '*.go'
(exit 1 — zero matches)
```

New member-tree acceptance tests (all PASS):

- TestVmPostApply_MemberTreeNodeDecodesClassifies — a converted distro-fedora-silhouette node (vm: primary + deploy-level pod member + in-substrate pod member) decodes into spec.FleetNode and classifies exactly: HasMembers, MemberByName, InSubstrateMembers/DeployLevelMembers with the position vocabulary.
- TestInGuestPodMembers_ConvertedShape — the vmPostApply walk deploys ONLY the in-substrate pod in-guest; the deploy-level pod (same target/image shape) never appears.
- TestInGuestPodMembers_SkipsNonPodAndImageless — image-less (agent-provisioned) and android/kubernetes/vm members filtered.
- TestVmPostApply_MemberTreeLegacyShapeIsHardCutover — a removed-shape node decodes member-less.

## Critical runtime check (config-parse surface)

The plugin NEVER re-reads charly.yml with its own embedded pin: its unified-file load goes through the executor (`loaderkit.LoadUnifiedViaExecutor` — the host charly owns the load AND the schema-CalVer version gate), and it consumes the MERGED node the host ships as JSON. So the in-repo proof of config acceptance is exactly the node decode+classify contract, proven by the new tests above against the converted shape. The file-stamp gate itself (2026.249.2125 vs the embedded `spec.SchemaVersion`) is charly-core-owned and NOT exercisable in-repo — the distro workers' bed R10s (check-omarchy-suite / cross-deployment beds) rerun against this new tag and are the live proof of the full parse surface.

## Rulebook compliance

- R0: plugin-repo task; no marketplace skill load required beyond the dispatcher's git-workflow row (followed: feat/ branch, PR-only).
- R1: RCA'd — the rejection root cause is the plugin's embedded spec parser predating the member-tree cutover; fixed at the pin + the one semantic consumer, not worked around.
- R3: member selection lives in ONE helper (`inGuestPodMembers`); no duplication of the position classification.
- R4: no sleeps/retries/hand-pins; pins are merged tags only (2106/2114 both merged releases).
- R5: `sortedChildKeys` and the dual-map walk deleted outright in the same PR.
- R6: feat/ branch, no force-push, no main push, no hook bypass; git status reviewed before commit.
- R7/R10: full gate (gofmt/build/vet/test -count=1) run on the final committed tree; sweep executed on the committed tree (git grep against HEAD).
- R8: no submodule edits — this IS the plugin repo (the umbrella records the gitlink via the pin-bump PR elsewhere).
- R9: no generated corpora touched; go.mod/go.sum hand-maintained pins.
- R10: evidence above from the committed tree, not an edited state.

## Change classification

- Change class: dependency re-pin + semantic consumer migration (Go source + tests), no pins/scripts/harness changes outside this repo.
- Verification gate: go build/vet/test -count=1 GREEN on the final tree + committed-tree identifier sweep; live config-parse proof delegated to the charly-side bed R10s (documented above).
- Attribution tier: fully tested and validated (gate passed on the final tree; the member-tree consumption path executed live via the new tests).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*