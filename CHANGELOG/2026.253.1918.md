# 2026.253.1918 — chore: re-pin sdk to v0.2026253.1714 — the canonical reference-resolution seam

## Summary

The deploy-add prepare-venue runs in this plugin's process, which pinned the OLD sdk (v0.2026252.834) — its load-time ValidateCheckBeds was the local-only lookup, so a bed cloning from a git-linked namespace golden (from: omarchy.omarchy-vm) validated GREEN in the kernel but FAILED in the deploy plugin ("references vm entity ... which is not defined"). The sdk bump brings the canonical ResolveEntityRef (sdk #246) + the namespace-aware runtime resolution (#247) into this process.

## How tested (pasted output)

**The live bed run with the NEW code (the PR branch injected via CHARLY_REPO_OVERRIDE — the disposable edge provision twin, ZERO failures):**

```
$ CHARLY_REPO_OVERRIDE="github.com/opencharly/plugin-deploy-vm=<this PR's branch>,github.com/opencharly/opencharly=<umbrella>" charly check run check-omarchy-eval-edge-inst
[vm-build] PASS after 4.48s
[vm-create] PASS after 3.95s
[deploy-add] PASS after 2m2.27s   ← the qualified ref resolves in THIS plugin's process (the fix)
[bring-up-members] PASS after 0s
[check-live] PASS after 1m7.451s
[update] PASS after 3m29.202s
[check-live-rebuild] PASS after 49.743s
[snapshot-capture] PASS after 157ms   ← the golden snapshot captured
charly check run check-omarchy-eval-edge-inst: PASS (steps=8)
```

The golden capture evidence:

```
snapshot-capture.log: created external snapshot "golden" on vm "omarchy.omarchy-vm"
  disk: .../charly-check-omarchy-eval-edge-inst/snapshots/golden/disk.qcow2
charly vm snapshot list: golden  external  2026-09-10T19:13:53Z  refcount 0
```

**The build + tests (pasted):**

```
$ go build ./...
(no output — clean)

$ go test ./...
ok  	github.com/opencharly/plugin-deploy-vm/candy/plugin-deploy-vm	0.004s
```

## Rulebook compliance

- R0 (skills first): the charly-vm:vm + charly-check:check skills govern this change — N/A to the diff itself
- R1 (RCA): the deploy-add failure was RCA'd (the plugin's stale sdk pin) before the fix
- R2 (no duplication): N/A — a dependency pin
- R3 (one implementation): the canonical ResolveEntityRef is the single surface
- R4 (no workarounds): the fix is the correct sdk pin
- R5 (delete legacy): the old local-only lookup is retired in the sdk
- R6 (git safety): PR-only landing
- R8 (no dead code): N/A
- R9 (no magic numbers): N/A
- R10 (fresh disposable proof): the live bed run above (the edge provision twin, disposable: true) executed the changed code path to ZERO failures — deploy-add PASS + the golden capture
- R10 bed-coverage matrix: the edge provision twin (check-omarchy-eval-edge-inst) is the disposable bed covering this change

## Change classification

- Change class: dependency re-pin (the reference seam)
- Verification gate: the live bed run (ZERO failures + the golden capture) + build/tests
- Attribution tier: fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*