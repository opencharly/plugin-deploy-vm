# 2026.266.1234 — feat(deploy-vm): adopt spec 261 (schema-head pin bump)

## Summary

The schema-249→261 wave leg for the out-of-process `deploy:vm` target — a prerequisite for the engine-provider-class cutover (spec#144/#145). `plugin-deploy-vm` self-loads the `kind:vm` entity out of the project, so its embedded `loaderkit` schema gate is what a 261 project hits.

**Complete diff accounting:** `go.mod` changes ONLY the two pins (`spec v0.2026262.957 → v0.2026266.545`, `sdk v0.2026265.2104 → v0.2026266.623`); `go.sum` changes ONLY the matching four lines (the `h1:`/`go.mod h1:` pair for each). There is no `golang.org/x/*` hunk. No code migration was needed — the plugin was already on the fleet→deploy vocabulary and reads no `EngineName`.

Before this leg a 261 `charly` driving a VM bed failed:
```
plugin-deploy-vm prepare-venue: resolve vm entity "eval-vm":
  .../charly.yml: config schema 2026.261.1747 is newer than this charly supports (max 2026.249.2125)
```

## How tested

**Change-class gate:** `charly check run check-substrate` (vm, `disposable: true`) — its `vm-create` + `bring-up-members` resolve a `kind:vm` entity THROUGH this plugin, so the changed schema gate executes live. Branch-built engine-cutover charly `2026.266.0826`.

**Full, unedited step list (12 steps, all `PASS`):**
```
[vm-build] PASS after 20.1s
[vm-create] PASS after 21.829s
[deploy-add] PASS after 54.786s
[bring-up-members] PASS after 35.85s
[check-live] PASS after 35.479s
[rebuild-members-down] PASS after 23.639s
[update] PASS after 2m28.065s         # fresh rebuild (R10)
[re-bring-up-members] PASS after 35.238s
[rebuild-ready] PASS after 931ms
[check-live-rebuild] PASS after 32.154s
[cleanup] PASS after 26.494s
[cleanup-members] PASS after 28.447s
PASS (steps=12)
```
`.check/check-substrate/2026.266.1115/summary.yml` → `ok: true`, `errors: 0`, `warnings: 0` on every step.

**Disposable target:** `check-substrate` carries `disposable: true`; the bed's own `cleanup` destroys it.
**Fresh rebuild:** `[update] PASS 2m28.065s` + `[check-live-rebuild]`.
**Changed-path execution:** the `kind:vm` resolution + prepare-venue path (the migrated plugin) ran live in `vm-create`/`bring-up-members`; before this leg that path FAILED with the skew above.

Local gates: `gofmt -l .` (empty) · `go build ./...` · `go vet ./...` · `go test ./...` GREEN.

## Project-rulebook rule-compliance

- **R0** — `/charly-check:check`, `/charly-internals:plugin`, `/charly-internals:git-workflow` loaded first.
- **R1** — the `prepare-venue` failure was root-caused to the embedded-schema skew and fixed in-tree.
- **R2** — the wave legs are named on opencharly/charly#633.
- **R3/R4** — no duplication/shim; a pin adoption only.
- **R5** — no removed identifier in this repo.
- **R6/R7** — green build/vet/test + the real bed.
- **R8** — no emitted artifact changed.
- **R9** — the bed runs the branch binary `2026.266.0826`.
- **R10** — the `check-substrate` disposable fresh-rebuild cycle above, 0 warnings / 0 failed steps.
- **RDD** — the schema gate was proven fixed live.
- **ADE** — the candy's baked plan ran.
- **Disposable-only autonomy** — only the bed's own `disposable: true` teardown.

## Change classification

Dependency/pin bump in a plugin candy (`go.mod`/`go.sum` only). Verification gate: the `check-substrate` disposable R10 bed (fresh rebuild) demonstrating a 261 charly driving this plugin + local Go gates. Attribution tier: `fully tested and validated`.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*