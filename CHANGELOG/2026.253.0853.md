# 2026.253.0853 — chore(deploy-vm): bump the spec pin to v0.2026253.825 (the WaitForSSH fast-fail fix)

## Summary

Bumps the spec pin to v0.2026253.825 — the WaitForSSH fast-fail fix (spec#126): the check-live hang regression. WaitForSSH aborts immediately on a permanent ssh failure (a missing managed alias) via ErrPollFatal instead of burning the whole readiness cap.

The bump carries the CC-2 fleet-vocabulary retirement (spec.FleetNode → spec.Deploy/DeployNode, UnifiedFile.Fleet → UnifiedFile.Deploy) — the lifecycle code + tests adapt to the renamed types.

## How tested (pasted)

```
$ go get github.com/opencharly/spec@v0.2026253.825
$ go get github.com/opencharly/sdk@latest
go: upgraded github.com/opencharly/sdk v0.2026249.2114 => v0.2026252.834
$ go build ./...
BUILD-OK
$ go test -count=1 ./...
ok  	github.com/opencharly/plugin-deploy-vm/candy/plugin-deploy-vm	0.014s
$ go vet ./... && gofmt -l .
VET-OK
FMT-DONE
```

The WaitForSSH fast-fail path (the changed code) is unit + integration tested in spec#126 (TestIsPermanentSSHFailure + TestWaitForSSH_MissingAliasFailsFast — a missing alias fails in ~3.8s against a 30s cap). The live check-live fast-fail lands with the plugin-check bump (the check-live engine's gate) + the charly rebuild.

## Rulebook compliance

- R0: git-workflow loaded.
- R3: only the spec + sdk pins are bumped (plus the required rename adaptation).
- R6: no force-push, no hook bypass, PR-only landing.
- R9: the pins are merged tags, never PR branches.
- R10: the build + tests pasted; the changed path's unit/integration proof referenced (spec#126).

## Change classification

- Change class: chore (pin bump + the rename adaptation)
- Verification gate: the build + tests (pasted) + the spec#126 unit/integration proof
- Attribution tier: analysed on a live system (the changed path's unit/integration proof is pasted in spec#126; the live check-live fast-fail lands with the plugin-check bump + the charly rebuild)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*