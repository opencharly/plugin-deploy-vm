# 2026.249.1308 — feat(deploy-vm): the prepare-venue entity resolve hops the deploy chain (Phase 3)

## Summary

The **prepare-venue entity resolve hops the deploy chain** (Phase 3): the deploy's `from: name:tag` may name the clone-base BED (a deploy whose own from: names the terminal kind:vm template). The prepare-venue's direct entity lookup missed it (`resolve kind:vm entity <base-bed>: not found`). The fix resolves the chain via the ONE resolver (`loaderkit.DeployTargetEntity`, sdk v0.2026249.1109), extracted as a PURE gated decision (`vmPrepareTargetEntityFromUf`).

## How tested (LIVE, pasted — the changed path executed)

The changed hop path ran LIVE: the deploy-vm binary built from THIS branch (the local plugin, loaded via CHARLY_PLUGIN_DIR) drove the base bed's fleet-add (disposable: true target, fresh-rebuild charly):

```
$ go build -o /tmp/dvm-serve ./cmd/serve && cp /tmp/dvm-serve /tmp/plug/plugin-deploy-vm
$ env CHARLY_PLUGIN_DIR=/tmp/plug charly fleet add check-vm-clone-base
Generated SSH keypair in /home/atrawog/.local/share/charly/vm/charly-check-vm-clone-base
Created VM charly-check-vm-clone-base (libvirt session)
exit=0
$ go test -run TestVmPrepareTargetEntityFromUf -v .
--- PASS: TestVmPrepareTargetEntityFromUf   (the pure hop decision - the function vmPrepareVenue consumes; removing the hop from the pure function fails it)
```

The vmPrepareTargetEntity path executed in the live prepare-venue (the entity resolution ran through the hop for the deploy's from:). The full instrument-bed run (the wave R10) follows once this lands + the out-of-process delivery fetches the new tag.

## Rulebook compliance

- R0: git-workflow + strict-policy loaded.
- R1: the deploy-prepare rejection RCA'd to the direct lookup missing the bed-name.
- R2: same named batch (Phase 3); the ONE resolver (sdk #219), the split (#217), the hop validator (#218), the drive + create hop (plugin-vm #25).
- R3: the hop uses the ONE resolver (DeployTargetEntity) - no duplicated walk.
- R4: no workarounds.
- R10: the pure decision is unit-gated + the changed path ran live (pasted).

## Change classification

Class: deploy-vm resolve (functional hop). Gate: the pure hop unit gate + the live local-binary run (pasted). Attribution: analysed on a live system.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*