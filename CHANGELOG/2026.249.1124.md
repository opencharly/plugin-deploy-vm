# 2026.249.1124 — chore(deploy-vm): pin sdk to the Phase 3 wave release

## Summary

Pin-only bump: **sdk v0.2026248.1522 → v0.2026249.1109** for the plugin-deploy-vm candy.

The Phase 3 from: name:tag wave's released chain: sdk 1109 carries the deploy-hop validator (a kind:check bed's from: may name ANOTHER kind:check bed — the clone-base pattern), the loader split, the ONE chain resolver, and the Docker cache. The deploy-vm's prepare-venue resolves the tree with this validator — the OLD pin (1522) predates the hop, so converted beds' `from: <base-bed>:golden` was rejected at deploy-prepare. No code change.

## How tested (pasted, released chain)

```
$ go build ./... && go test -count=1 .
ok  	github.com/opencharly/plugin-deploy-vm/candy/plugin-deploy-vm
$ # the charly binary built from the RELEASED chain (no local replaces):
$ charly version
2026.249.0739
$ charly box validate    # the converted distro-cachyos tree
charly box validate: OK — checked 505 candies, 40 boxes, 36 deploys, 7 distros, 7 builders; 0 warnings, 0 errors
```

The instrument bed's vm-build (the from-snapshot drive) previously passed live in run 2026.249.1032 (`Wrote output/qcow2/check-vm-clone-base/disk.qcow2 (clone of check-vm-clone-base@golden)`); the deploy-add path is what this pin unblocks.

## Rulebook compliance

- R4: no workarounds — the pin resolves the published module (real go.sum entries, no local replaces).
- R2: same named batch (Phase 3); sdk #217/#218/#219 + spec #101/#102 are the merged legs.
- R1: the old pin's validator gap RCA'd (the deploy-prepare rejection).
- R10: the released-chain validation pasted above.

## Change classification

Class: dependency pin bump (mechanical). Gate: build + unit + the released-chain validation (pasted). Attribution: fully tested and validated.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*