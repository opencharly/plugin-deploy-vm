# 2026.250.1108 — fix: vm rebuild + ephemeral lifecycle use the CC-2 deploy word (not fleet)

## Summary

**CC-2 sweep residual: the vm substrate still shells the RETIRED `charly fleet add` in its rebuild path.** The plan's claim-keyed sweep (`git grep -iw fleet` → ZERO outside historical CHANGELOGs) is violated here — the pod twin was already fixed in opencharly/plugin-deploy-pod#11 (tag v2026.250.1020); the VM twin was missed, and the R10 final-binary bed matrix surfaced it live: every VM-bed `charly check run` update (fresh-rebuild) leg fails with `the "fleet" command is retired in the CC-2 vocabulary cutover — use charly deploy add / charly deploy del instead (fleet → deploy)` (exit 80).

This PR retires the word in every remaining shell + dispatch + doc in this plugin:

- `lifecycle.go` `vmRebuild`: `vmCli(..., "fleet", "add", p.Name)` → `"deploy", "add"` — the CLI seam that `charly update <vm-bed>` routes through (the R10-hit call site).
- `lifecycle.go` nested-pod leg: `"%s fleet from-box %s %s"` → `"%s deploy from-box %s %s"` (the deploy surface's from-box leg).
- `lifecycle.go` ephemeral provider dispatches: `InvokeProvider("command", "fleet", OpEphemeralRegister/Teardown)` → `"deploy"` — plugin-fleet's command provider re-registered under the `deploy` word, so the old word would fail to resolve a provider.
- `lifecycle_test.go` + `ephemeral_register_test.go` + comment alignment: assertions and fixtures now expect the `deploy` word (error-text fixture mirrors plugin-fleet's current `deploy ephemeral-register:` prefix).

Dispositioned: `lifecycle_hop_test.go:18` `Fleet: map[string]spec.FleetNode` — a spec-contract type name at this plugin's pinned spec v0.2026249.2129 (which still aliases `FleetNode = Deploy`); contract vocabulary is renamed by the spec repo's own rebuild, out of scope for this consumer fix (the pod twin's #11 was also a single-file consumer change).

## How tested

**Unit gates (go build && go vet && go test):**

```
$ cd candy/plugin-deploy-vm && go build ./... && go vet ./... && go test -count=1 ./...
ok  	github.com/opencharly/plugin-deploy-vm/candy/plugin-deploy-vm	0.014s
?   	github.com/opencharly/plugin-deploy-vm/candy/plugin-deploy-vm/cmd/serve	[no test files]
```

**LIVE FRESH-REBUILD BED RUN (A2/B17) — `disposable: true` two-VM-member bed `check-r10-two-vm` (authored `disposable: true` in the CC-R10 matrix project charly.yml), run from the final charly binary v2026.250.1014 with `CHARLY_REPO_OVERRIDE=github.com/opencharly/plugin-deploy-vm=/tmp/cc2vm` pointing at THIS fix.** Pre-fix the exact same bed failed its update leg (exit 80, retired fleet word); post-fix the full 12-step fresh-rebuild sequence passes, including the `update` leg that routes through vmRebuild's changed `deploy add` shell:

```
$ CHARLY_PROJECT_DIR=/tmp/cc-r10 CHARLY_REPO_OVERRIDE=github.com/opencharly/plugin-deploy-vm=/tmp/cc2vm charly check run check-r10-two-vm   # target check-r10-two-vm: disposable: true
charly check run check-r10-two-vm: [vm-build-r10-vm-member] PASS after 1.551s [allowlisted=1]
charly check run check-r10-two-vm: [vm-build] PASS after 779ms [allowlisted=1]
charly check run check-r10-two-vm: [vm-create] PASS after 522ms [allowlisted=1]
charly check run check-r10-two-vm: [deploy-add] PASS after 23.407s [allowlisted=1]
charly check run check-r10-two-vm: [bring-up-members] PASS after 32.197s
charly check run check-r10-two-vm: [check-live] PASS after 4.537s [allowlisted=1]
charly check run check-r10-two-vm: [rebuild-members-down] PASS
charly check run check-r10-two-vm: [update] PASS after 37.197s [allowlisted=5]
charly check run check-r10-two-vm: [re-bring-up-members] PASS after 1m53.109s
charly check run check-r10-two-vm: [check-live-rebuild] PASS after 3.174s [allowlisted=1]
charly check run check-r10-two-vm: [cleanup] PASS after 1.671s
charly check run check-r10-two-vm: [cleanup-members] PASS after 4.98s
charly check run check-r10-two-vm: PASS (steps=12)
$ echo $?
0
```

The `update` step (37.197s) is the very leg that failed pre-fix — it routes through `vmRebuild`'s `charly deploy add` (lifecycle.go:728), now exec'd live at exit 0. Both members' marker checks pass in check-live (two-VM-member bed: `r10-primary-marker` + `r10-member-marker` — member steps recorded and asserted concurrently per the CC parallel walk). The bed's `vm:` node carries `disposable: true` (the standing R10 authorization for the destroy/rebuild legs).

**Sweep after fix** (only the dispositioned spec-contract name remains; the `plugin-fleet` repo/package NAME is not a retired word):

```
$ grep -rniw "fleet" --include="*.go" . | grep -v ".git/" | grep -v "plugin-fleet"
./candy/plugin-deploy-vm/lifecycle_hop_test.go:18:   Fleet: map[string]spec.FleetNode{
```

## Rulebook compliance

- **R1**: root-caused live (the R10 final-binary bed matrix failed at the update leg with `the "fleet" command is retired … (fleet → deploy)` exit 80; trace: `deploy-dispatch rebuild → InvokeProvider deploy:vm op=rebuild → vmCli "fleet" "add"` — the CC-2 sweep miss, pod twin plugin-deploy-pod#11 already merged).
- **R3**: the fix mirrors plugin-deploy-pod#11's exact argv shape (one canonical `deploy add` surface); no duplicated shell surface.
- **R4**: no workaround — the retired word dies in this plugin; beds pass through the real `deploy` surface.
- **R5**: the old word is removed completely across this plugin's shells, dispatches, tests, and docs; the dispositioned `spec.FleetNode` alias is contract vocabulary at the pinned spec version, not a shell.
- **R6**: feat/-branch, PR-only; no force-push, no main push, hooks intact.
- **R7**: gates above are paste-evidence for the change class.
- **R10**: the fresh-rebuild bed run above IS the live proof of the changed path (run on the final binary + this fix via the dev-local override, exit 0, `disposable: true` target).

## Change classification

- Change class: **CC-2 consumer fix** (retired-word sweep residual).
- Verification gate: **unit gates (build/vet/test) + live fresh-rebuild bed run** (A2 satisfied — the changed `deploy add` path executed live at exit 0 in the repro bed above; B17 satisfied — the target `check-r10-two-vm` is authored `disposable: true`).
- Attribution tier: **analysed on a live system** (the changed path ran live on the final binary via the dev-local override; the full post-tag matrix re-run lands in the CC-R10 wave).
- Assisted-by: *pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*